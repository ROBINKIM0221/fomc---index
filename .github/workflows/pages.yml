name: Build & Deploy Pages (Serverless)

on:
  workflow_dispatch:
  schedule:
    - cron: '30 0 * * *'   # 매일 09:30 KST

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Cache HuggingFace models
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: ${{ runner.os }}-hf-${{ hashFiles('builder/sentiment.py') }}
          restore-keys: |
            ${{ runner.os }}-hf-

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        env:
          PIP_DISABLE_PIP_VERSION_CHECK: "1"
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pipeline (ONLINE)
        env:
          # 분석 백엔드 토글: finbert(기본) | llm
          # llm 사용 시 repo secret 으로 ANTHROPIC_API_KEY 를 등록하세요.
          SENTIMENT_BACKEND: "finbert"
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_MODEL: "claude-opus-4-8"
          HF_HOME: ~/.cache/huggingface
          TOKENIZERS_PARALLELISM: "false"
          FINBERT_BATCH: "16"
          FINBERT_MAXLEN: "256"
          KEEP_N: "60"        # 최근 N개만 추론(속도/요금 절충). 필요시 늘리세요.
        run: python -m builder.build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
